<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Pos_app" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/brand_purple</item>
        <item name="colorPrimaryContainer">@color/brand_purple</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Solid status bar = same as View Details -->
        <item name="android:statusBarColor">@color/brand_purple</item>
        <!-- Light icons (time, battery) on purple -->
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>